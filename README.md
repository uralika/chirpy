# Run Project Locally:

## Back-End:
In the project's main folder:
1. `$ npm install`
2. `$ npm start`

## Front-End:
In a separate tab of the terminal, `cd client`:
1. `$ npm install` 
2. `$ npm start`
